import React from 'react';
import SmallInputWithIcon from "@/components/smallInputWithIcon/smallInputWithIcon";

const SearchByTags = () => {
    return (
        <div className={'container mx-auto mt-[35px] bg-grey px-[30px] py-[20px] border-[3px] border-strokeColor rounded-[32px]'}>
        </div>
    );
};

export default SearchByTags;